# Data pipeline Python dependencies
# Installed in the Airflow container via _PIP_ADDITIONAL_REQUIREMENTS
# Also used locally for running scripts directly

amadeus==9.0.0
psycopg2-binary==2.9.10
python-dotenv==1.0.1
dbt-postgres==1.9.1

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
